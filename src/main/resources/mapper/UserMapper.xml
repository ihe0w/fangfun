<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.fangfun.dao.UserDao">
    <resultMap id="userMap" type="com.example.fangfun.model.User" autoMapping="true">
        <id property="userId" jdbcType="INTEGER" column="user_id"/>
        <result property="userName" jdbcType="VARCHAR" column="user_name"/>

    </resultMap>

    <select id="selectUserByAccount" parameterType="String" resultMap="userMap">
        SELECT * FROM user WHERE user_name = #{account}
    </select>

    <select id="selectPasswordById" parameterType="int" resultType="String">
        SELECT password FROM user WHERE user_id = #{userId}
    </select>
    <select id="selectPasswordByAccount" parameterType="String" resultType="String">
        SELECT password FROM user WHERE user_name=#{account}
    </select>

    <insert id="insertUser" parameterType="com.example.fangfun.model.User">
        <selectKey keyProperty="userId" order="AFTER" resultType="int">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into user (user_id, user_name, password,
        phone, email)
        values (#{userId,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR},
        #{phone,jdbcType=VARCHAR}, #{email,jdbcType=VARCHAR})
    </insert>
</mapper>
